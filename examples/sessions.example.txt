# Recommended option.
# Put your Pyrogram .session files inside the repo's sessions folder.
# This active line tells the bot to load every .session file from that folder:
sessions

# You can also load one exact .session file:
# sessions\account_01.session

# Or use one Pyrogram session string per line:
# 1AABCD_your_pyrogram_session_string_here
