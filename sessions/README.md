# Sessions Folder

Put Pyrogram `.session` files in this folder.

Example:

```text
sessions/account_01.session
sessions/account_02.session
```

The default `sessions.txt` created by `setup.bat` already contains:

```text
sessions
```

That means the bot will automatically load every `.session` file from this folder.
